# PDFly — core engine (Python 3.9+)
Flask==3.1.3
pypdf==6.16.2
reportlab==4.5.1
Pillow>=10.0
pypdfium2>=4.28
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1
xhtml2pdf>=0.2.16
pytesseract>=0.3.13
deep-translator>=1.11
striprtf>=0.0.27
beautifulsoup4>=4.12
pdf2docx>=0.5.8        # layout-preserving PDF -> Word (pulls PyMuPDF)

# System packages (outside pip):
#   tesseract-ocr            -> OCR (apt install tesseract-ocr)
#   ghostscript              -> PDF/A conversion (apt install ghostscript)
#   poppler-utils            -> pdf rendering support (apt install poppler-utils)
